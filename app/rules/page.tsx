import { PageShell } from "@/components/page-shell";

export default function RulesPage() {
  return (
    <PageShell
      title="议事规则模块"
      description="当前为受限占位页，后续由 Gemini 完成正式内容页。"
    />
  );
}
